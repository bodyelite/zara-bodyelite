services:
  - type: web
    name: zara-bodyelite1
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: WHATSAPP_TOKEN
        sync: false
      - key: PORT
        value: 10000
