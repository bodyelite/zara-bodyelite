import express from 'express';
import cors from 'cors';
import dotenv from 'dotenv';
import { procesarEvento, getSesiones, toggleBot, enviarMensajeManual } from './app.js';

dotenv.config();
const app = express();
app.use(express.json());
app.use(cors());

app.get('/monitor', (req, res) => {
    res.send(`<!DOCTYPE html><html lang="es"><head><meta charset="UTF-8"><title>ZARA BONITA</title><link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet"><style>:root { --bg: #111b21; --sidebar: #202c33; --header: #202c33; --incoming: #202c33; --outgoing: #005c4b; --text: #e9edef; --accent: #00a884; } * { box-sizing: border-box; } body { margin: 0; font-family: 'Inter', sans-serif; background: var(--bg); color: var(--text); display: flex; height: 100vh; overflow: hidden; } .sidebar { width: 350px; background: var(--sidebar); border-right: 1px solid #333; display: flex; flex-direction: column; } .card { padding: 12px 15px; border-bottom: 1px solid #2a3942; cursor: pointer; display: flex; align-items: center; gap: 15px; } .card.active { background: #2a3942; border-left: 4px solid var(--accent); } .avatar { width: 45px; height: 45px; background: #667781; border-radius: 50%; display: flex; align-items: center; justify-content: center; } .tag { font-size: 0.7em; padding: 2px 6px; border-radius: 4px; font-weight: bold; } .tag.CALIENTE { background: #ff0044; } .tag.INTERESADO { background: #ff9900; } .tag.NUEVO { background: var(--accent); } .tag.FRIO { background: #667781; } .main { flex: 1; display: flex; flex-direction: column; background: #0b141a; } .feed { flex: 1; padding: 20px; overflow-y: auto; display: flex; flex-direction: column; gap: 8px; background-image: url('https://user-images.githubusercontent.com/15075759/28719144-86dc0f70-73b1-11e7-911d-60d70fcded21.png'); } .msg { max-width: 65%; padding: 8px 12px; border-radius: 8px; font-size: 0.95em; position: relative; } .msg.user { align-self: flex-start; background: var(--incoming); } .msg.assistant { align-self: flex-end; background: var(--outgoing); } .msg-time { font-size: 0.7em; opacity: 0.6; text-align: right; } .input-area { padding: 15px; background: var(--header); display: flex; gap: 10px; } input { flex: 1; background: #2a3942; border: none; color: white; padding: 12px; border-radius: 8px; outline: none; } .btn-toggle { background: transparent; border: 1px solid var(--accent); color: var(--accent); padding: 5px 15px; border-radius: 20px; cursor: pointer; } .btn-toggle.off { border-color: #ff0044; color: #ff0044; }</style></head><body><div class="sidebar"><div style="padding:15px; font-weight:600; border-bottom:1px solid #333">ZARA BONITA</div><div id="list"></div></div><div class="main"><div id="h" style="padding:10px 20px; background:var(--header); display:none; justify-content:space-between; align-items:center"><div id="t"></div><button id="b" class="btn-toggle" onclick="toggle()">ZARA: ON</button></div><div id="feed" class="feed"></div><div id="i" class="input-area" style="display:none"><input id="m" placeholder="Mensaje..." onkeypress="if(event.key==='Enter')send()"><button onclick="send()" style="background:var(--accent); border:none; padding:10px; border-radius:50%; color:white; cursor:pointer">➤</button></div></div><script>let us={}, ap=null; function update(){fetch("/api/data").then(r=>r.json()).then(d=>{us=d.users; renderList(); if(ap)renderChat(ap);});} function renderList(){ const l=document.getElementById("list"); l.innerHTML=""; Object.keys(us).sort((a,b)=>(us[b].lastInteraction||0)-(us[a].lastInteraction||0)).forEach(p=>{ const u=us[p]; const div=document.createElement("div"); div.className="card "+(ap===p?"active":""); div.onclick=()=>select(p); div.innerHTML=\`<div class="avatar">\${u.name[0]}</div><div style="flex:1"><div style="display:flex; justify-content:space-between"><b>\${u.name}</b><span class="tag \${u.tag}">\${u.tag}</span></div><div style="font-size:0.8em; opacity:0.6">\${(u.history[u.history.length-1]?.content||"").substring(0,30)}</div></div>\`; l.appendChild(div);});} function select(p){ap=p; document.getElementById("h").style.display="flex"; document.getElementById("i").style.display="flex"; document.getElementById("t").innerText=us[p].name; update();} function renderChat(p){ const f=document.getElementById("feed"); f.innerHTML=""; us[p].history.forEach(m=>{ const d=document.createElement("div"); d.className="msg "+m.role; const t=new Date(m.timestamp).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'}); d.innerHTML=\`<div>\${m.content}</div><div class="msg-time">\${t}</div>\`; f.appendChild(d);}); f.scrollTop=f.scrollHeight;} async function toggle(){await fetch("/api/toggle",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({phone:ap})}); update();} async function send(){const i=document.getElementById("m"); const v=i.value; if(!v||!ap)return; i.value=""; await fetch("/api/manual",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({phone:ap,text:v})}); update();} setInterval(update,2000); update();</script></body></html>`);
});
app.get('/api/data', (req, res) => res.json({ users: getSesiones() }));
app.post('/api/toggle', (req, res) => { toggleBot(req.body.phone); res.json({ success: true }); });
app.post('/api/manual', async (req, res) => { await enviarMensajeManual(req.body.phone, req.body.text); res.json({ success: true }); });
app.get('/webhook', (req, res) => { if (req.query['hub.verify_token'] === 'zara123') res.send(req.query['hub.challenge']); else res.sendStatus(403); });
app.post('/webhook', async (req, res) => { try { await procesarEvento(req.body.entry?.[0]); res.sendStatus(200); } catch (e) { res.sendStatus(500); } });
app.listen(process.env.PORT || 3000);
